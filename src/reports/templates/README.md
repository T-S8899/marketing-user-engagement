# Report Templates

Markdown report templates and reusable narrative fragments can live here when reporting requirements become more formal.

The current report builder creates deterministic Markdown directly from local processed and analysis CSV files.
